# 服务器配置
PORT=3000
HOST=localhost
CORS=true
ENABLE_DEMO=true

# 浏览器配置
HEADLESS=true
TIMEOUT=30000

# 默认截图设置
DEFAULT_WIDTH=1920
DEFAULT_HEIGHT=1080
SCREENSHOT_QUALITY=90

# CLI 工具配置（用于远程 API 调用）
SCREENSHOT_API_URL=http://localhost:3000/screenshot

# 环境
NODE_ENV=development
